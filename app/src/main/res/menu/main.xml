<?xml version="1.0" encoding="utf-8"?>
<menu
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        tools:showIn="navigation_view">

    <group android:checkableBehavior="single">

        <item
            android:id="@+id/mainMenuAccount"
            android:icon="@drawable/ic_account"
            android:title="Your account"/>

        <item
            android:id="@+id/mainMenuArchivedMessages"
            android:icon="@drawable/ic_save"
            android:title="Archive"/>

        <item
            android:id="@+id/mainMenuSettings"
            android:icon="@drawable/ic_settings"
            android:title="Settings"/>

    </group>

    <item android:title="App info">
        <menu>
            <item
                android:id="@+id/mainMenuPrivacyPolicy"
                android:icon="@drawable/ic_security"
                android:title="Privacy policy"/>

            <item
                android:id="@+id/mainMenuAbout"
                android:icon="@drawable/ic_info"
                android:title="About app"/>

        </menu>
    </item>

</menu>