<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="com.peteralexbizjak.chatapp_android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
            android:allowBackup="false"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.auth.WelcomeActivity"/>
        <activity android:name=".activities.auth.SignUpActivity"/>
        <activity android:name=".activities.channel.ChatActivity"/>
        <activity android:name=".activities.channel.FindUsers"/>
        <activity android:name=".activities.navigation.AccountActivity"/>
        <activity android:name=".activities.navigation.ArchiveActivity"/>
        <activity android:name=".activities.navigation.SettingsActivity"/>
        <activity android:name=".activities.info.PrivacyPolicyActivity"/>
        <activity android:name=".activities.info.AboutActivity"/>
    </application>

</manifest>